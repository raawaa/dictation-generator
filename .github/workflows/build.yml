name: Build and Release Windows EXE

on:
  push:
    tags:
      - 'v*.*.*'  # 当推送版本标签时触发（如 v0.1.0）
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: windows-latest

    permissions:
      contents: write  # 需要写入权限以创建 Release

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Extract version from tag
      id: get_version
      run: |
        if ("${{ github.ref }}" -like "refs/tags/*") {
          $version = "${{ github.ref }}" -replace "refs/tags/", "" -replace "^v", ""
          echo "version=$version" >> $env:GITHUB_OUTPUT
          echo "Version: $version"
        } else {
          echo "version=dev" >> $env:GITHUB_OUTPUT
        }
      shell: pwsh

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install PyQt6 reportlab pyinstaller

    - name: Build with PyInstaller
      run: |
        pyinstaller build.spec

    - name: Rename EXE with version
      id: rename
      run: |
        $version = "${{ steps.get_version.outputs.version }}"
        if ($version -eq "dev") {
          $version = "dev"
        }
        $oldName = Get-ChildItem -Path "dist" -Filter "*.exe" | Select-Object -First 1 -ExpandProperty Name
        $newName = "dictation-generator-windows-$version.exe"
        Rename-Item -Path "dist\$oldName" -NewName $newName
        echo "exe_name=$newName" >> $env:GITHUB_OUTPUT
        echo "Renamed to: $newName"
      shell: pwsh

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: dictation-generator-windows
        path: dist/*.exe
        retention-days: 30

    - name: Generate Changelog
      id: changelog
      run: |
        $version = "${{ steps.get_version.outputs.version }}"
        echo "Generating changelog for version $version"
        
        # 获取上一个版本标签
        $prevTag = $(git describe --tags --abbrev=0 HEAD^ 2>$null)
        if ($LASTEXITCODE -ne 0) {
          $prevTag = ""
        }
        echo "Previous tag: $prevTag"
        
        # 获取自上一个版本以来的提交
        if ($prevTag) {
          $commits = git log $prevTag..HEAD --pretty=format:"%h|%s|%an|%ad" --date=short
        } else {
          $commits = git log --pretty=format:"%h|%s|%an|%ad" --date=short
        }
        
        # 解析提交信息并分类
        $features = @()
        $fixes = @()
        $docs = @()
        $chores = @()
        $others = @()
        
        foreach ($commit in $commits) {
          if ($commit -match '^(.+)\|(.+)\|(.+)\|(.+)$') {
            $hash = $matches[1]
            $message = $matches[2]
            $author = $matches[3]
            $date = $matches[4]
            
            # 解析 Conventional Commits 格式
            if ($message -match '^(feat|fix|docs|chore|refactor|style|test|build|ci|perf|revert)(\(.+\))?:\s*(.+)') {
              $type = $matches[1]
              $scope = $matches[2]
              $desc = $matches[3]
              
              $entry = "- $desc ($hash)"
              
              switch ($type) {
                'feat' { $features += $entry }
                'fix' { $fixes += $entry }
                'docs' { $docs += $entry }
                'chore' { $chores += $entry }
                'refactor' { $others += $entry }
                'style' { $others += $entry }
                'test' { $others += $entry }
                'build' { $others += $entry }
                'ci' { $others += $entry }
                'perf' { $others += $entry }
                'revert' { $others += $entry }
              }
            } else {
              $others += "- $message ($hash)"
            }
          }
        }
        
        # 生成变更日志
        $releaseBody = @()
        $releaseBody += "## 英语单词默写纸生成器 v$version"
        $releaseBody += ""
        $releaseBody += "### 下载"
        $releaseBody += "- Windows 版本：`dictation-generator-windows-$version.exe`"
        $releaseBody += ""
        $releaseBody += "### 功能特性"
        $releaseBody += "- 从 CSV 文件读取词汇数据"
        $releaseBody += "- 支持按单元、类型筛选"
        $releaseBody += "- 随机抽取指定数量词汇"
        $releaseBody += "- 生成 PDF 格式默写纸"
        $releaseBody += "- 图形化界面操作"
        $releaseBody += ""
        $releaseBody += "### 使用说明"
        $releaseBody += "1. 下载 EXE 文件"
        $releaseBody += "2. 双击运行"
        $releaseBody += "3. 选择 CSV 文件和配置参数"
        $releaseBody += "4. 点击生成按钮即可"
        $releaseBody += ""
        $releaseBody += "### 系统要求"
        $releaseBody += "- Windows 10 或更高版本"
        $releaseBody += ""
        $releaseBody += "## 变更日志"
        $releaseBody += ""
        
        if ($features.Count -gt 0) {
          $releaseBody += "### ✨ 新功能"
          $releaseBody += ""
          $features | ForEach-Object { $releaseBody += $_ }
          $releaseBody += ""
        }
        
        if ($fixes.Count -gt 0) {
          $releaseBody += "### 🐛 问题修复"
          $releaseBody += ""
          $fixes | ForEach-Object { $releaseBody += $_ }
          $releaseBody += ""
        }
        
        if ($docs.Count -gt 0) {
          $releaseBody += "### 📝 文档更新"
          $releaseBody += ""
          $docs | ForEach-Object { $releaseBody += $_ }
          $releaseBody += ""
        }
        
        if ($chores.Count -gt 0) {
          $releaseBody += "### 🔧 构建/工具"
          $releaseBody += ""
          $chores | ForEach-Object { $releaseBody += $_ }
          $releaseBody += ""
        }
        
        if ($others.Count -gt 0) {
          $releaseBody += "### 其他"
          $releaseBody += ""
          $others | ForEach-Object { $releaseBody += $_ }
          $releaseBody += ""
        }
        
        if ($features.Count -eq 0 -and $fixes.Count -eq 0 -and $docs.Count -eq 0 -and $chores.Count -eq 0 -and $others.Count -eq 0) {
          $releaseBody += "无变更记录"
        }
        
        # 将变更日志保存到文件，使用换行符连接
        $releaseBody -join "`n" | Out-File -FilePath "release_body.txt" -Encoding UTF8
        
        # 输出前几行用于调试
        Get-Content "release_body.txt" | Select-Object -First 20
      shell: pwsh

    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')  # 仅在推送标签时创建 Release
      uses: softprops/action-gh-release@v1
      with:
        files: dist/*.exe
        name: Release ${{ steps.get_version.outputs.version }}
        body_path: release_body.txt
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}